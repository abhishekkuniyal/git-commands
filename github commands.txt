git commands

git config --global user.email "email"
git config -- global user.name " username"

git init 
git add <filename>
git add .

git -m commit "message"
git status 

git log --oneline

git ingnore 

git keep 

git diff
 
 git diff --staged


git stash list


git tag

git switch "branchname"

git branch

git checkout "brachname"

for conflicts:-  git commit -m "Merge remote-tracking branch 'origin/main'"


git merge 

git remote -v
git remote add origin "link"

git push -u origin main
 
